.w-full.flex.flex-col
  .my-8
    %h1.font-bold.text-2xl= @course.name

    = @course.description

  
  - if current_user.course_inscript?(@course)
    .my-4
      = render @course.sections
    .flex.gap-4.items-baseline
      = button_to 'Retirar Inscripcion', students_course_inscriptions_path(@course), method: :delete, class: "mt-2 rounded-lg py-3 px-5 bg-red-200 font-medium", form: { data: { turbo_confirm: 'Si te retiras del curso se borarran todos tus progresos. ¿Seguro que quieres continuar?' } }
      = link_to 'Regresar', students_courses_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 border border-black inline-block font-medium"

  - else
    .flex.gap-4.items-baseline
      = button_to 'Inscribir', students_course_inscriptions_path(@course), { class: 'rounded-lg py-3 px-5 bg-purple-600 text-white block font-medium' }

      = link_to 'Regresar', students_courses_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 border border-black inline-block font-medium"

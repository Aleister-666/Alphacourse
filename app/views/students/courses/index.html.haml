.w-full

  .flex.justify-between.items-center
    %h1.font-bold.text-4xl Courses
  .min-w-full.grid.grid-col-2.md:grid-col-3.lg:grid-col-4
    - @courses.each do |course|
      .p-12.bg-purple-200.rounded.flex.flex-col
        = link_to course.name, students_course_path(course), class: 'text-purple-600 hover:underline hover:text-purple-800'

        - unless current_user.course_inscript?(course)
          = button_to 'Inscribir', students_course_inscriptions_path(course, current_user), { class: 'rounded-lg py-3 px-5 bg-purple-600 text-white block font-medium' }
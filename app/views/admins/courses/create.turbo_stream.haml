= turbo_stream.update dom_id(Course.new), ''
= turbo_stream.prepend 'courses_content', partial: 'admins/courses/course', locals: { course: @course }
= turbo_stream.replace 'modal-window' do
	= render(Modal::WindowComponent.new(id: 'modal-window', header: 'Registrar Curso')) do
		.p-6
			= turbo_frame_tag dom_id(Course.new), src: new_admins_course_path

			
= render_flash_messages